sudo: required
services:
  - docker 

script: 
  - docker build -t dheerajodha/node .
  - docker images dheerajodha/node

before_deploy:
  - docker login -u dheerajodha -p @*yVru)E4M+xiL2

deploy:
  provider: script
  script: docker push dheerajodha/node
  on:
    branch: master